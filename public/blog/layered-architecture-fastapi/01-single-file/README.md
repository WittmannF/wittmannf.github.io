# 01 - Single File

The whole app lives in `main.py`: HTTP, schemas, retrieval, fake news fetching, prompt assembly, and answer generation.

This is fine for a demo, but every change touches the same file.
