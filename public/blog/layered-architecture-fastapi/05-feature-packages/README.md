# 05 - Feature Packages

When one feature becomes large, each layer inside the feature can become a package.

This is useful when a feature has multiple routers, multiple use cases, multiple persistence mechanisms, or multiple external integrations.
